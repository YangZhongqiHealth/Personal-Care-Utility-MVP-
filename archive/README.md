# Archive

The archive is for local legacy material that should not be treated as part of the current publishable codebase.

In this workspace, `old_pcu_simple_using_tom/` remains available for reference but is excluded from the default GitHub-ready scope.
