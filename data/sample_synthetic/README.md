# Sample Synthetic Data

This folder is reserved for a small sanitized example dataset that can be safely committed to GitHub.

A good sample should:

- contain only a minimal number of participant-days
- preserve the file layout expected by the MVP
- demonstrate the `cg_*` and `lon_*` provenance fields
- avoid including raw source-study dumps
